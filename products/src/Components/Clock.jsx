import React,{useState,useEffect, use} from 'react';

function Clock() {

    const [time, setTime] = useState(new Date());
    //in this line time is a state variable and setTime is a function to update the state variable

    useEffect


}